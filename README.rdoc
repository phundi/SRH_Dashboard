Dependencies
	Ruby on rails >= 2.1.2
	Rails 4.2.6

Setup:
	Command: bundle install --local
	Command: bundle exec rake db:create db:migrate db:seed

Runnning Application
	Command: passenger start

Add to nginx for production ENV
